{
  "name": "userland-examples",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "userland": "tsx cli/src/index.ts",
    "test": "npm run validate:catalog && npm run validate:manifests && npm run validate:skills && vitest run",
    "validate:catalog": "tsx scripts/validate-catalog.ts",
    "validate:manifests": "tsx scripts/validate-manifests.ts",
    "validate:skills": "tsx scripts/validate-skills.ts"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "tsx": "^4.19.4",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
