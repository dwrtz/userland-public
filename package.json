{
  "name": "userland-public",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "userland": "tsx cli/src/index.ts",
    "cli:test": "vitest run cli/tests",
    "test": "npm run validate:catalog && npm run validate:manifests && npm run validate:skills && npm run validate:links && npm run validate:cli && npm run cli:test && vitest run examples scripts",
    "typecheck": "tsc --noEmit",
    "validate:catalog": "tsx scripts/validate-catalog.ts",
    "validate:cli": "tsx scripts/validate-cli.ts",
    "validate:links": "tsx scripts/validate-links.ts",
    "validate:manifests": "tsx scripts/validate-manifests.ts",
    "validate:skills": "tsx scripts/validate-skills.ts"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "tsx": "^4.19.4",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
